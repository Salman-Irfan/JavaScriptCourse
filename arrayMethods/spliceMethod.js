const myArray = [1,2,3,4,5];
const a = myArray.splice(1,2,"one")
// splice(1,2,"one")
// splice(index at which new insertion, number after how many will be deleted, insert array)
console.log(myArray)
console.log(a)